version: "2"
sql:
  - engine: "postgresql"
    queries: "./internals/sql/query.sql"
    schema: "./internals/sql/schema.sql"
    gen:
      go:
        package: "models"
        out: "./internals/models"
        sql_package: "pgx/v5"
